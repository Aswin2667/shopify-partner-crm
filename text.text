project where flag not = true  

run throu loop fr each 

function 
    if oldDate && oldDomain exist {
        query  use maxoccured at
    event = event[0]
    event emit
    
    }else{
        first 100

    }
