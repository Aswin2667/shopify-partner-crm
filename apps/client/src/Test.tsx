import React, { useEffect } from 'react';

export default function Component() {
  // useEffect(() => {
  //   if (window.AnalyticsFactory) {
  //     const analytics = window.AnalyticsFactory.create("Shopify");
  //     if (typeof analytics.page === 'function') {
  //       analytics.page("HomePage");
  //     } else {
  //       console.error("page method is not available on the analytics instance.");
  //     }
  //   } else {
  //     console.error("AnalyticsFactory is not available on the global window object.");
  //   }
  // }, []);

  return <>hello</>;
}
